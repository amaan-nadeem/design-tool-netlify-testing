// Components
import LeftNav from "./LeftNav";

// Styled
import { StyledToolbar } from "./styled";

const Toolbar = () => {
  return (
    <StyledToolbar>
      <LeftNav />
    </StyledToolbar>
  );
};

export default Toolbar;
